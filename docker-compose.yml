services:
  backend:
    build: ./backend
    environment:
      PORT: 5000
    expose:
      - "5000"

  frontend:
    build:
      context: ./frontend
      args:
        REACT_APP_API_URL: /api
    ports:
      - "80:80"
    depends_on:
      - backend
